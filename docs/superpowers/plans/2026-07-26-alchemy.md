# Alchemy Publication Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace Paper #4 with *Alchemy: A Formal Theory of Consciousness, Embodiment, and Materialization*, release it through a renamed `dm3n/alchemy` repository, and update the complete O-1A portfolio and itinerary.

**Architecture:** The existing LaTeX paper remains the source and visual foundation, but its argument is reorganized so alchemy is the governing causal theory. Existing empirical citations and documentary sources are retained where relevant, the unsupported-claims table is removed, and one new identity-convergence result is added. The paper repository is released first; the O-1A portfolio then consumes its final title, URL, metrics, and checksum.

**Tech Stack:** LaTeX, BibTeX, TikZ, Poppler (`pdfinfo`, `pdftotext`, `pdftoppm`, `pdfunite`), Git, GitHub CLI, shell checksum tools.

## Global Constraints

- Title exactly: *Alchemy: A Formal Theory of Consciousness, Embodiment, and Materialization*.
- Daniel Ray Edgar remains sole author.
- Use ASCII hyphens in authored prose and do not add AI attribution.
- Preserve both supplied source PDFs byte-for-byte.
- Do not add a claim-audit table.
- Treat Gateway and the patent as primary documentary records, not independent scientific replication.
- Treat kundalini, sacred secretion, Christ consciousness, theosis, and Hermeticism seriously as transformative maps while distinguishing interpretation from established physiology.
- Keep the paper professional, concise, mathematically explicit, and consistent with Daniel's other papers.
- Complete the GitHub, O-1A folder, technical portfolio, itinerary, and Brain memory updates.

---

### Task 1: Lock the source and evidence map

**Files:**
- Modify: `refs.bib`
- Modify: `sources/README.md`
- Create: `docs/research/alchemy-source-map.md`

**Interfaces:**
- Consumes: `sources/US6506148.pdf`, `sources/cia-RDP96-00788R001700210016-5.pdf`, and the current `refs.bib`.
- Produces: a citation-key map and explicit documentary-use rules consumed by `main.tex`.

- [ ] **Step 1: Record baseline source hashes**

Run:

```bash
shasum -a 256 sources/US6506148.pdf sources/cia-RDP96-00788R001700210016-5.pdf
```

Expected:

```text
370c090ca861d7c9797af9ef11b57bbe7f7051da2bfa5a59adc88592037b360c  sources/US6506148.pdf
dff63304a51a547ae41141006314e67c7f100822621748b97192f64b09c6a05e  sources/cia-RDP96-00788R001700210016-5.pdf
```

- [ ] **Step 2: Add only the identity and self-transformation references needed by the new theorem**

Add peer-reviewed or canonical records for identity-based motivation, self-perception through enacted behavior, and bounded reinforcement updates. Use DOI or stable publisher metadata and cite only claims the sources establish.

- [ ] **Step 3: Write the source map**

`docs/research/alchemy-source-map.md` must map each paper section to exact citation keys and define:

- empirical mechanism;
- formal result;
- primary-document statement;
- interpretive synthesis.

It must identify the Gateway memorandum's Army authorship and CIA archive custody, the patent's claims and legal status, and the exact role of the Human Divinity manuscript.

- [ ] **Step 4: Rewrite the source README**

Retitle it for *Alchemy*, preserve source metadata and hashes, and explain that Gateway is used as an example of technological alchemy while the patent is used as a proposed nervous-system modulation mechanism.

- [ ] **Step 5: Verify the source archive is unchanged**

Run the hash command from Step 1 and require exact equality.

- [ ] **Step 6: Commit the evidence foundation**

```bash
git add refs.bib sources/README.md docs/research/alchemy-source-map.md
git commit -m "Build the evidence foundation for Alchemy"
```

---

### Task 2: Rewrite the manuscript around alchemy

**Files:**
- Modify: `main.tex`

**Interfaces:**
- Consumes: the approved design, source map, bibliography, and archived primary records.
- Produces: the complete single-source LaTeX manuscript for `main.pdf`.

- [ ] **Step 1: Replace publication identity and abstract**

Set the exact title and rewrite the abstract around this definition:

```text
Alchemy is the disciplined process by which an imaginal form is embodied,
enacted, and stabilized as material reality.
```

The abstract must name the causal chain, four formal results, inner alchemy, technological alchemy, and the practical Great Work.

- [ ] **Step 2: Rebuild the narrative in ten sections**

Use these exact section titles:

```text
Alchemy Is the Human Operation
The Three Realms
The Human Vessel
The Mathematics of Materialization
Solve et Coagula
Inner Alchemy
Technological Alchemy
The Counter-Alchemical Environment
The Great Work
Become the Part
```

Retain the strongest current material, but move it under the new alchemical grammar.

- [ ] **Step 3: Add the identity-convergence theorem**

Define an embodied identity state \(Z_t\), enacted identity-consistent signal \(U_t\), and update:

```tex
Z_{t+1}=(1-\alpha)Z_t+\alpha U_t,\qquad 0<\alpha\le 1.
```

For constant enacted target \(U_t=z^\star\), prove:

```tex
Z_t=(1-\alpha)^t Z_0+\left[1-(1-\alpha)^t\right]z^\star
\longrightarrow z^\star.
```

Explain the boundary: rehearsal becomes identity transformation only when it repeatedly enters enacted behavior and feedback.

- [ ] **Step 4: Reframe the retained formal results**

Rename their surrounding exposition with alchemical language while preserving mathematical correctness:

- mediation as the operative channel from form to matter;
- reachable futures as expansion of possible material states;
- repetition as cumulative transmutation through attempts.

- [ ] **Step 5: Replace both diagrams**

Create two TikZ diagrams:

1. `The Alchemical Loop`: imaginal form -> vessel -> policy -> material change -> assay -> revised form.
2. `Solve et Coagula`: inherited form -> dissolution -> imagined target -> embodiment -> enactment -> coagulated reality.

No table environments may remain.

- [ ] **Step 6: Integrate inner and technological alchemy**

The Inner Alchemy section must connect:

- Hermetic correspondence and mental form;
- Jungian psychic transformation;
- kundalini and internal ascent;
- sacred secretion as Daniel's related interpretive physiology;
- Christ consciousness and theosis as transformation into a divine pattern;
- the body as ritual laboratory.

The Technological Alchemy section must explain:

- Gateway's Hemi-Sync, frequency-following, Focus states, visualization, patterning, and experimental recommendations;
- why the memorandum can be read as an attempt to engineer states that older traditions cultivated through ritual and discipline;
- the patent's pulsed-monitor-field proposal and stated sensory-resonance mechanism;
- the difference between formal technical treatment and replicated efficacy.

- [ ] **Step 7: Write the Great Work protocol and conclusion**

Use eight operations:

1. Name the material form.
2. Dissolve the inherited pattern.
3. Construct the imaginal form.
4. Rehearse the identity and process.
5. Prepare the vessel.
6. Build the supporting environment.
7. Enact and produce physical evidence.
8. Assay, refine, and repeat.

End with Daniel's thesis that playing the part, when joined to embodiment, action, and feedback, can transform the actor and the world the actor is able to build.

- [ ] **Step 8: Run manuscript integrity checks**

Run:

```bash
rg -n '\\\\begin\\{table|\\\\begin\\{tabular|claim.audit|From Vision to Reality' main.tex
rg -n 'TODO|TBD|PLACEHOLDER|Citation needed|undefined' main.tex
```

Expected: no matches.

- [ ] **Step 9: Commit the manuscript**

```bash
git add main.tex
git commit -m "Rewrite Paper Four as Alchemy"
```

---

### Task 3: Compile and visually verify the paper

**Files:**
- Modify: `main.pdf`
- Temporary: `tmp/pdfs/alchemy-*`

**Interfaces:**
- Consumes: `main.tex`, `refs.bib`.
- Produces: verified publication PDF and release metrics.

- [ ] **Step 1: Compile through BibTeX**

Run:

```bash
pdflatex -interaction=nonstopmode -halt-on-error main.tex
bibtex main
pdflatex -interaction=nonstopmode -halt-on-error main.tex
pdflatex -interaction=nonstopmode -halt-on-error main.tex
pdflatex -interaction=nonstopmode -halt-on-error main.tex
```

Expected: every command exits 0.

- [ ] **Step 2: Check the log**

Run:

```bash
rg -n 'LaTeX Warning|undefined references|Citation.*undefined|Overfull|Underfull|multiply defined' main.log
```

Fix all undefined references, citation errors, and material overfull boxes. Accept only harmless underfull bibliography spacing.

- [ ] **Step 3: Extract and inspect publication metrics**

Run:

```bash
pdfinfo main.pdf
pdftotext main.pdf tmp/pdfs/alchemy.txt
wc -w tmp/pdfs/alchemy.txt
rg -o '\\\\cite[tp]?\\{[^}]+\\}' main.tex
shasum -a 256 main.pdf
```

Record page count, extracted word count, unique cited-source count, theorem count, diagram count, and SHA-256.

- [ ] **Step 4: Render every page**

Run:

```bash
mkdir -p tmp/pdfs
pdftoppm -png -r 150 main.pdf tmp/pdfs/alchemy
```

Inspect every rendered page for clipping, overlap, missing glyphs, broken equations, diagram legibility, page-number consistency, blank pages, and bad section breaks.

- [ ] **Step 5: Recompile after any visual fix**

Repeat Steps 1 through 4 until the latest render has zero visible defects.

- [ ] **Step 6: Clean LaTeX intermediates and commit the publication**

```bash
rm -f main.aux main.bbl main.blg main.log main.out
git add main.pdf
git commit -m "Publish Alchemy"
```

---

### Task 4: Rewrite repository publication materials

**Files:**
- Modify: `README.md`
- Modify: `.git/config` through `git remote set-url`

**Interfaces:**
- Consumes: final publication metrics and PDF checksum.
- Produces: a complete public repository landing page for the Alchemy release.

- [ ] **Step 1: Rewrite the README**

The README must contain:

- exact title and author;
- direct PDF link;
- one-paragraph thesis;
- alchemical causal chain;
- four formal results;
- inner-alchemy synthesis;
- Gateway and patent role;
- eight-operation Great Work;
- final release metrics;
- build instructions and repository contents.

Do not include a claim-audit section or the old title.

- [ ] **Step 2: Verify every repository reference uses `dm3n/alchemy`**

Run:

```bash
rg -n 'embodied-agency-hypothesis|From Vision to Reality' README.md sources/README.md main.tex
```

Expected: no matches.

- [ ] **Step 3: Commit and push the release under the current remote**

```bash
git add README.md sources/README.md
git commit -m "Prepare the Alchemy repository release"
git push origin main
```

- [ ] **Step 4: Rename the GitHub repository**

Run:

```bash
gh repo rename alchemy --repo dm3n/embodied-agency-hypothesis --yes
git remote set-url origin https://github.com/dm3n/alchemy.git
git remote -v
gh repo view dm3n/alchemy --json name,url,defaultBranchRef
```

Expected: repository name `alchemy`, URL `https://github.com/dm3n/alchemy`, default branch `main`.

- [ ] **Step 5: Rename the local repository**

From `/Users/dm3n/Projects`:

```bash
mv embodied-agency-hypothesis alchemy
```

Continue all remaining work from `/Users/dm3n/Projects/alchemy`.

- [ ] **Step 6: Verify the renamed repository**

Run:

```bash
git -C /Users/dm3n/Projects/alchemy status --short --branch
git -C /Users/dm3n/Projects/alchemy ls-remote --heads origin main
```

Expected: clean `main`, remote `main` present.

---

### Task 5: Replace the canonical O-1A Paper #4 bundle

**Files:**
- Rename: `/Users/dm3n/Desktop/O-1A/Academic Papers/4 - From Vision to Reality`
- Create: `/Users/dm3n/Desktop/O-1A/Academic Papers/4 - Alchemy/Alchemy.pdf`
- Modify copied repository files inside the renamed bundle.

**Interfaces:**
- Consumes: final files from `/Users/dm3n/Projects/alchemy`.
- Produces: canonical O-1A Academic Paper #4 bundle.

- [ ] **Step 1: Rename the bundle directory**

Run:

```bash
mv "/Users/dm3n/Desktop/O-1A/Academic Papers/4 - From Vision to Reality" \
   "/Users/dm3n/Desktop/O-1A/Academic Papers/4 - Alchemy"
```

- [ ] **Step 2: Replace the bundle contents**

Copy the final `main.tex`, `refs.bib`, `README.md`, source README, and exact source PDFs from the repository. Copy `main.pdf` as `Alchemy.pdf`. Remove only the superseded `From Vision to Reality.pdf` inside the renamed Paper #4 folder.

- [ ] **Step 3: Verify canonical equality**

Run:

```bash
cmp /Users/dm3n/Projects/alchemy/main.pdf \
  "/Users/dm3n/Desktop/O-1A/Academic Papers/4 - Alchemy/Alchemy.pdf"
cmp /Users/dm3n/Projects/alchemy/main.tex \
  "/Users/dm3n/Desktop/O-1A/Academic Papers/4 - Alchemy/main.tex"
shasum -a 256 \
  "/Users/dm3n/Desktop/O-1A/Academic Papers/4 - Alchemy/sources/US6506148.pdf" \
  "/Users/dm3n/Desktop/O-1A/Academic Papers/4 - Alchemy/sources/cia-RDP96-00788R001700210016-5.pdf"
```

Expected: `cmp` exits 0 and source hashes match the baseline.

---

### Task 6: Update and rebuild the O-1A portfolio

**Files:**
- Rename: `/Users/dm3n/Projects/daniel-edgar-ai-portfolio/docs/11-research-embodied-agency.md`
- Create: `/Users/dm3n/Projects/daniel-edgar-ai-portfolio/docs/11-research-alchemy.md`
- Modify: `/Users/dm3n/Projects/daniel-edgar-ai-portfolio/README.md`
- Modify: `/Users/dm3n/Projects/daniel-edgar-ai-portfolio/docs/08-o1a-evidence-map.md`
- Modify: `/Users/dm3n/Projects/daniel-edgar-ai-portfolio/Daniel_Edgar_Technical_Portfolio.tex`
- Modify: `/Users/dm3n/Projects/daniel-edgar-ai-portfolio/Daniel_Edgar_Technical_Portfolio.pdf`
- Modify: `/Users/dm3n/Desktop/O-1A/Itinerary + portfolio.pdf`

**Interfaces:**
- Consumes: final paper title, URL, metrics, theorem list, and checksum.
- Produces: synchronized O-1A article, technical portfolio, evidence map, and itinerary.

- [ ] **Step 1: Rename and rewrite the research article**

Rename the file to `docs/11-research-alchemy.md`. Rewrite it around:

- Daniel's original definition of human alchemy;
- the three realms;
- the four formal results;
- solve et coagula;
- inner and technological alchemy;
- the Great Work;
- exact release metrics and `dm3n/alchemy` link.

- [ ] **Step 2: Update every portfolio reference**

Replace the old title, article path, repo slug, abstract, theorem count, metrics, and claim-audit references in `README.md`, `docs/08-o1a-evidence-map.md`, and `Daniel_Edgar_Technical_Portfolio.tex`.

- [ ] **Step 3: Verify no stale references remain**

Run:

```bash
rg -n 'From Vision to Reality|embodied-agency-hypothesis|11-research-embodied-agency|claim.audit' \
  README.md docs Daniel_Edgar_Technical_Portfolio.tex
```

Expected: no matches.

- [ ] **Step 4: Compile the technical portfolio**

Run:

```bash
pdflatex -interaction=nonstopmode -halt-on-error Daniel_Edgar_Technical_Portfolio.tex
pdflatex -interaction=nonstopmode -halt-on-error Daniel_Edgar_Technical_Portfolio.tex
```

Expected: both commands exit 0 with no undefined references.

- [ ] **Step 5: Rebuild the itinerary**

Run:

```bash
./build-itinerary.sh
```

Expected: `/Users/dm3n/Desktop/O-1A/Itinerary + portfolio.pdf` is rebuilt with the technical portfolio followed by the one-page resume.

- [ ] **Step 6: Render and inspect both PDFs**

Render every page of `Daniel_Edgar_Technical_Portfolio.pdf` and `Itinerary + portfolio.pdf` with `pdftoppm`. Verify the Alchemy section, repository URL, page breaks, final resume page, and absence of clipping or blank pages.

- [ ] **Step 7: Commit and push the portfolio**

```bash
git add README.md docs/08-o1a-evidence-map.md docs/11-research-alchemy.md \
  Daniel_Edgar_Technical_Portfolio.tex Daniel_Edgar_Technical_Portfolio.pdf
git add -u docs/11-research-embodied-agency.md
git commit -m "Update the O-1A portfolio for Alchemy"
git push origin main
```

---

### Task 7: Final release audit and memory update

**Files:**
- Modify: Brain memory for Paper #4.
- Modify: Brain `Memory/MEMORY.md` pointer.

**Interfaces:**
- Consumes: final paper and portfolio commits, hashes, page counts, and URLs.
- Produces: a durable release record and clean handoff.

- [ ] **Step 1: Run the final paper audit**

From `/Users/dm3n/Projects/alchemy`:

```bash
git status --short --branch
git log -1 --oneline --decorate
gh repo view dm3n/alchemy --json name,url,defaultBranchRef
pdfinfo main.pdf
shasum -a 256 main.pdf sources/*.pdf
rg -n 'From Vision to Reality|embodied-agency-hypothesis|claim.audit' \
  README.md main.tex sources/README.md
```

Expected: clean synchronized `main`, correct GitHub identity, valid PDF metadata, preserved source hashes, and no stale references.

- [ ] **Step 2: Run the final O-1A audit**

From `/Users/dm3n/Projects/daniel-edgar-ai-portfolio`:

```bash
git status --short --branch
git log -1 --oneline --decorate
pdfinfo Daniel_Edgar_Technical_Portfolio.pdf
pdfinfo "/Users/dm3n/Desktop/O-1A/Itinerary + portfolio.pdf"
shasum -a 256 Daniel_Edgar_Technical_Portfolio.pdf \
  "/Users/dm3n/Desktop/O-1A/Itinerary + portfolio.pdf"
rg -n 'From Vision to Reality|embodied-agency-hypothesis|11-research-embodied-agency' \
  README.md docs Daniel_Edgar_Technical_Portfolio.tex
```

Expected: clean synchronized `main`, valid PDFs, final hashes recorded, and no stale references.

- [ ] **Step 3: Update Brain memory**

Update Paper #4 memory with:

- new title and thesis;
- local and GitHub repo names;
- canonical O-1A folder and PDF;
- section architecture and four formal results;
- paper and portfolio metrics;
- final commit hashes and PDF SHA-256 values;
- preserved primary-source hashes;
- evidence boundaries;
- prior edition recovery commit.

Update `Memory/MEMORY.md` so its Paper #4 pointer uses *Alchemy*.

- [ ] **Step 4: Report the release**

Report the live repository URL, paper path, O-1A bundle path, final metrics, verification results, commit hashes, and any remaining non-blocking caveats.
